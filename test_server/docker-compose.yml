version: '3.8'

services:
  vulnerable-web:
    build: .
    container_name: cal-test-target
    ports:
      - "8082:80"
    restart: unless-stopped
    environment:
      - TZ=Asia/Seoul
    networks:
      - cai-net

networks:
  cai-net:
    driver: bridge
